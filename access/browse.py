import webbrowser

# Ask the user for the website domain or IP address
url = input("Enter the URL to open in browser: ")

# Open the default web browser and direct it to the specified URL
webbrowser.open(url)
